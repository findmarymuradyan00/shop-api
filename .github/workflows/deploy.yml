name: Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Copy docker-compose.yml to VM
        run: cp docker-compose.yml ~/my-app/

      - name: Stop old containers
        run: cd ~/my-app && docker compose down || true

      - name: Start containers
        run: cd ~/my-app && docker compose up -d --build